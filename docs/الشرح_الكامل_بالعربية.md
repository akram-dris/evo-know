# الشرح الكامل للمشروع بالعربية

## أولاً: ما هو هذا المشروع؟

هذا مشروع تخرج ماستر في هندسة البرمجيات بجامعة سعد دحلب - البليدة 1، تحت إشراف الأستاذة شيخي إيمان.

**العنوان الرسمي**: استخدام تقنيات الذكاء الاصطناعي لتنفيذ نظام قابل للتشغيل البيني لإدارة المعرفة المؤسسية المبني على الحوسبة السحابية.

**الفكرة ببساطة**: بناء نظام ذكي يساعد المؤسسات على تحديث معارفها تلقائياً باستخدام الذكاء الاصطناعي، مع واجهة Slack كنقطة تفاعل للموظفين، وبنية Cloud Native (خدمات مصغرة).

---

## ثانياً: من أين جاء هذا المشروع؟ (السياق الأكاديمي)

### 2.1 أطروحة الدكتوراه للمشرفة
الأستاذة شيخي إيمان اقترحت في أطروحتها إطار عمل (Framework) لإدارة المعرفة المؤسسية مبني على Cloud Computing. هذا الإطار يشمل 7 عمليات فرعية:
1. **رصد المعرفة** (Repérage) ← تم تنفيذه في مشاريع ماستر سابقة
2. **اكتساب المعرفة** (Acquisition) ← تم تنفيذه سابقاً
3. **صياغة المعرفة** (Formalisation) ← تم تنفيذه سابقاً
4. **مشاركة المعرفة** (Partage) ← نفذته إكرام في المذكرة السابقة
5. **استخدام المعرفة** (Utilisation) ← لم يُنفذ بعد
6. **تحديث المعرفة** (Mise à jour) ← **هذا هو مشروعنا نحن!**
7. **البيئة المؤسسية** (Environnement) ← لم يُنفذ بعد

### 2.2 مذكرة إكرام (العمل السابق المباشر)
إكرام بنت نظام لمشاركة المعرفة باستخدام Spring Boot + Eureka + Docker + Prometheus. أهم ما قدمته نظرياً:
- **تعريف أول** لعملية تحديث المعرفة، قسمتها إلى 4 عمليات فرعية:
  - الإثراء (Enrichissement)
  - التغيير والتنقيح (Changement & Raffinement)
  - التقييم (Évaluation)
  - التطوير (Développement)
- لكن تعريفها كان **محوره الإنسان فقط** بدون ذكاء اصطناعي

### 2.3 مساهمتنا الجديدة (ملف 1.docx)
في ملف الـ docx أضفنا:
- **جدول حالة الفن**: 25+ بحث علمي عن AI × KM (من 2015 إلى 2025)
- **جدول التقنيات**: ربط كل عملية فرعية بتقنيات IA محددة
- **العملية الفرعية الخامسة**: الأتمتة والتنسيق بالذكاء الاصطناعي (Orchestration) ← هذا ابتكارنا الجديد

---

## ثالثاً: المهام الخمس المختارة للتنفيذ

المشرفة قالت بوضوح في docs.txt: مستحيل تنفيذ كل شيء. اختارت 5 مهام محددة:

### المهمة 1: التنبؤ باحتياجات التحديث (T1)
- **من أي عملية فرعية؟** التقييم
- **ماذا تفعل؟** تتوقع متى ستصبح وثيقة معينة قديمة/متقادمة
- **تقنيات الذكاء الاصطناعي**: LSTM (شبكة عصبية)، Prophet (فيسبوك)، ARIMA
- **كيف تعمل؟**
  - تأخذ بيانات: عمر الوثيقة، عدد مرات الوصول، اتجاه الاستخدام
  - تُدرّب نموذج سلسلة زمنية (Time Series)
  - تُخرج "نقاط خطر التقادم" من 0.0 (جديدة) إلى 1.0 (قديمة جداً)
- **التطبيق العملي بالكود**:
  - `services/t1-prediction/` ← مجلد الخدمة المصغرة
  - `models/prophet_model.py` ← نموذج Prophet
  - `models/lstm_model.py` ← نموذج LSTM بـ PyTorch
  - `scoring.py` ← حساب النقاط النهائية (مزيج مرجّح من عدة عوامل)
  - المعادلة: `النقطة = 0.3 × عامل_العمر + 0.5 × عامل_الاتجاه + 0.2 × عامل_الاستبدال`

### المهمة 2: التوليد التلقائي لتقارير التحديث (T2)
- **من أي عملية فرعية؟** التقييم
- **ماذا تفعل؟** تولّد تقارير مكتوبة بلغة طبيعية تلقائياً
- **تقنيات الذكاء الاصطناعي**: LLM (نماذج اللغة الكبيرة مثل Gemini)، NLG
- **أنواع التقارير**:
  - تقرير أسبوعي (كل جمعة تلقائياً)
  - تقرير تنبيه (عند اكتشاف وثيقة متقادمة)
  - تقرير دمج (بعد دمج معارف مكررة)
  - تقرير تناقض (بعد اكتشاف تناقضات)
  - تقرير اكتشاف (عند اكتشاف علاقات جديدة)
- **التطبيق**: نرسل البيانات المجمعة لـ Gemini API مع prompt مهيكل → يُرجع تقرير Markdown جاهز

### المهمة 3: الدمج الذكي للمعارف (T3)
- **من أي عملية فرعية؟** التغيير والتنقيح
- **ماذا تفعل؟** تكتشف المعارف المكررة وتدمجها تلقائياً
- **تقنيات الذكاء الاصطناعي**: K-Means، DBSCAN (تجميع دلالي)، إزالة التكرار بـ NLP
- **كيف تعمل؟**
  1. تأخذ كل embeddings من قاعدة FAISS
  2. تحسب التشابه الدلالي (cosine similarity)
  3. تُجمّع القطع المتشابهة بـ DBSCAN (العتبة: 0.85)
  4. لكل مجموعة فيها أكثر من عنصر → تستخدم LLM لدمجها في نص واحد شامل
  5. تُحدّث قاعدة البيانات وقاعدة المعرفة (Graph)

### المهمة 4: التحليل التلقائي للتناسق (T4)
- **من أي عملية فرعية؟** التغيير والتنقيح
- **ماذا تفعل؟** تكتشف التناقضات بين المعارف المخزنة
- **تقنيات الذكاء الاصطناعي**: NLI (Natural Language Inference)، محركات استدلال، Knowledge Graphs
- **طريقتان متكاملتان**:
  - **الطريقة 1 (NLI)**: نموذج `nli-deberta-v3-base` يأخذ زوج نصوص → يصنفها: تأكيد/حياد/تناقض
  - **الطريقة 2 (Graph)**: استعلامات Cypher على Neo4j لاكتشاف تناقضات هيكلية
- **تحسين**: لا نفحص كل الأزواج (O(n²))، بل فقط الأزواج المتشابهة دلالياً (نفس الموضوع)

### المهمة 5: الاكتشاف التلقائي للمعارف (T5)
- **من أي عملية فرعية؟** التطوير
- **ماذا تفعل؟** تكتشف علاقات مخفية بين المفاهيم
- **تقنيات الذكاء الاصطناعي**: NER، Apriori/FP-Growth، GNN
- **ثلاث تقنيات متكاملة**:
  - **NER**: استخراج الكيانات (أشخاص، تقنيات، عمليات) باستخدام CamemBERT (يدعم الفرنسية)
  - **Association Rules**: اكتشاف أنماط التلازم (Apriori) ← "الوثائق التي تذكر الموضوع A غالباً تذكر الموضوع B أيضاً"
  - **Link Prediction**: التنبؤ بروابط مفقودة في الـ Knowledge Graph

---

## رابعاً: البنية التقنية (Technology Stack)

| الطبقة | التقنية | لماذا؟ |
|--------|---------|--------|
| لغة البرمجة | Python 3.10+ | المعيار في AI/ML |
| إطار الويب | FastAPI | سريع، async، يولد توثيق API تلقائياً |
| واجهة المستخدم | Slack (slack_bolt) | بوت محادثة مؤسسي |
| وسيط الرسائل | Apache Kafka | Event Bus بين الخدمات (كما في المخطط) |
| قاعدة بيانات اتجاهية | FAISS | بحث دلالي سريع |
| قاعدة معرفة هيكلية | Neo4j | علاقات بين المفاهيم |
| قاعدة بيانات علائقية | PostgreSQL | البيانات الوصفية وسجلات التدقيق |
| مكتبات AI | Transformers, Sentence-Transformers, scikit-learn, Prophet | الـ embeddings والنماذج |
| LLM | Gemini API | توليد التقارير وRAG |
| الحاويات | Docker + Docker Compose | بنية Cloud Native |
| المراقبة | Prometheus + ELK | كما في مخطط المشرفة |

---

## خامساً: بنية المشروع (هيكل الملفات)

```
km-update-system/
├── docker-compose.yml          # تنسيق كل الخدمات
├── .env                        # المتغيرات السرية
│
├── services/
│   ├── api-gateway/            # البوابة الرئيسية (FastAPI)
│   ├── t1-prediction/          # المهمة 1: التنبؤ
│   ├── t2-report-generation/   # المهمة 2: التقارير
│   ├── t3-knowledge-fusion/    # المهمة 3: الدمج
│   ├── t4-consistency-check/   # المهمة 4: التناسق
│   ├── t5-knowledge-discovery/ # المهمة 5: الاكتشاف
│   ├── slack-bot/              # بوت سلاك
│   └── orchestrator/           # العملية الفرعية الخامسة
│
├── shared/                     # كود مشترك
│   ├── database/               # اتصالات Postgres, Neo4j, FAISS
│   ├── embeddings/             # نموذج الـ embeddings
│   └── kafka/                  # منتج ومستهلك Kafka
│
└── data/                       # البيانات الخام والمعالجة
```

---

## سادساً: قواعد البيانات

### PostgreSQL (9 جداول):
1. **documents** ← كل وثيقة مُدخلة (العنوان، القسم، الحالة)
2. **knowledge_chunks** ← القطع النصية المعالجة + الـ embeddings
3. **obsolescence_scores** ← نتائج المهمة 1 (نقاط التقادم)
4. **update_reports** ← التقارير المولدة من المهمة 2
5. **fusion_events** ← سجل عمليات الدمج من المهمة 3
6. **consistency_issues** ← التناقضات المكتشفة من المهمة 4
7. **discovered_relations** ← العلاقات المكتشفة من المهمة 5
8. **audit_log** ← سجل تدقيق XAI (تتبع كل قرار آلي)
9. **access_logs** ← سجلات الوصول (لتدريب نموذج التنبؤ)

### Neo4j (Knowledge Graph):
- **العقد**: Document, Concept, Department, Employee
- **العلاقات**: CONTAINS_CONCEPT, BELONGS_TO, RELATED_TO, CONTRADICTS, SUPERSEDES

---

## سابعاً: الخطة الزمنية (4 أسابيع)

### الأسبوع 1: البنية التحتية
- **يوم 1**: إنشاء المشروع + Git + هيكل المجلدات
- **يوم 2**: كتابة Dockerfiles + docker-compose.yml + تشغيل البنية التحتية
- **يوم 3**: تصميم مخططات PostgreSQL + Neo4j + إنشاء الجداول
- **يوم 4**: بناء pipeline الإدخال (تحليل PDF/DOCX → تقطيع النص)
- **يوم 5**: توليد الـ embeddings + تخزينها في FAISS + ربطها بالـ Graph
- **عطلة**: اختبار + تحضير 20+ وثيقة تجريبية

### الأسبوع 2: نواة الذكاء الاصطناعي
- **يوم 1**: المهمة 1 (Prophet + LSTM للتنبؤ بالتقادم)
- **يوم 2**: المهمة 2 (Gemini API لتوليد التقارير)
- **يوم 3**: المهمة 3 (DBSCAN + LLM للدمج الذكي)
- **يوم 4**: المهمة 4 (NLI DeBERTa + Cypher لفحص التناسق)
- **يوم 5**: المهمة 5 (CamemBERT NER + Apriori للاكتشاف)
- **عطلة**: ربط الخدمات الخمس عبر Kafka + اختبار شامل

### الأسبوع 3: Slack + التنسيق الآلي
- **يوم 1**: إنشاء تطبيق Slack + الأوامر الأساسية (/km-search, /km-status)
- **يوم 2**: بناء RAG Pipeline (سؤال → embedding → بحث FAISS → Gemini → إجابة)
- **يوم 3**: الإشعارات التفاعلية (أزرار: مراجعة/أرشفة/رفض)
- **يوم 4**: بناء الـ Orchestrator (العملية الخامسة) ← فحص يومي تلقائي + حل النزاعات
- **يوم 5**: التقارير الآلية عبر Slack (تقرير أسبوعي كل جمعة)
- **عطلة**: ذاكرة محادثة + معالجة الأخطاء

### الأسبوع 4: التشغيل البيني + النشر + التقييم
- **يوم 1**: REST API خارجي + JWT + Webhooks (لربط أنظمة المؤسسة)
- **يوم 2**: تحضير Docker الإنتاجي + NGINX + SSL
- **يوم 3**: النشر على السحابة (Google Cloud أو VM محلي)
- **يوم 4**: 10 سيناريوهات اختبار + مقاييس جودة AI
- **يوم 5**: التوثيق النهائي + مخططات UML + تحضير العرض
- **عطلة**: تقديم عرض توضيحي للمشرفة

---

## ثامناً: تدفق البيانات (كيف تتحرك المعلومات)

```
مصادر المعرفة الخارجية (ملفات PDF, API, سجلات)
    ↓
بوابة API (FastAPI) ← نقطة الدخول الوحيدة
    ↓
ناقل الأحداث (Kafka) ← يوزع الأحداث للخدمات
    ↓
┌──────────────────────────────────────────────┐
│ document.ingested (وثيقة جديدة أُدخلت)       │
│   ├→ T5: استخراج الكيانات (NER)              │
│   ├→ T3: فحص التكرار                         │
│   ├→ T4: فحص التناسق                         │
│   └→ T1: تحديث نقاط التقادم                   │
│                                               │
│ discovery.found (علاقة جديدة اكتُشفت)         │
│   └→ T2: إضافة للتقرير القادم                 │
│                                               │
│ fusion.completed (دمج تم)                     │
│   ├→ T4: إعادة فحص التناسق                    │
│   └→ T2: إضافة للتقرير                       │
│                                               │
│ consistency.checked (تناقض اكتُشف)             │
│   ├→ Orchestrator: حل تلقائي أو تصعيد         │
│   └→ T2: إضافة للتقرير                       │
│                                               │
│ prediction.scored (نقطة تقادم عالية)           │
│   ├→ Orchestrator: إرسال تنبيه                │
│   └→ T2: إضافة للتقرير                       │
│                                               │
│ report.generated (تقرير جاهز)                  │
│   └→ Slack Bot: نشر في #km-updates            │
└──────────────────────────────────────────────┘
```

---

## تاسعاً: بوت Slack بالتفصيل

### الأوامر المتاحة:
- `/km-search [سؤال]` ← بحث دلالي في قاعدة المعرفة
- `/km-status` ← حالة النظام (عدد الوثائق، التنبيهات، المشاكل)
- `/km-report` ← توليد تقرير فوري
- `/km-predict [اسم وثيقة]` ← فحص نقطة تقادم وثيقة

### RAG Pipeline (كيف يجيب البوت على الأسئلة):
1. الموظف يسأل: `@KM Bot ما هي سياسة النسخ الاحتياطي؟`
2. النظام يحول السؤال إلى vector (embedding)
3. يبحث في FAISS عن أقرب 5 قطع نصية
4. يرسل القطع + السؤال لـ Gemini API
5. Gemini يولد إجابة مع ذكر المصادر
6. البوت ينشر الإجابة في Slack مع أزرار تقييم (مفيد/غير مفيد)

### القنوات:
- `#km-updates` ← التقارير الأسبوعية
- `#km-alerts` ← تنبيهات فورية
- `#km-discovery` ← اكتشافات جديدة
- `#km-general` ← أسئلة وأجوبة

---

## عاشراً: العملية الفرعية الخامسة (الابتكار الرئيسي)

هذا هو **الإسهام الأصلي** لهذا المشروع. الـ Orchestrator يعمل كعقل مركزي:

1. **الجدولة**: فحص يومي الساعة 2 صباحاً (T1→T5→T3→T4→T2)
2. **المراقبة المستمرة**: كل 30 دقيقة يفحص إن كانت هناك وثائق خطيرة
3. **حل النزاعات تلقائياً**:
   - إذا أحد النصين أحدث بـ 6+ أشهر → يُفضَّل الأحدث
   - إذا الثقة منخفضة → يُصعَّد للإنسان عبر Slack
   - إذا معقد → يُستخدم LLM للتحكيم
4. **التتبع (XAI)**: كل قرار يُسجَّل في audit_log مع شرح بلغة طبيعية

---

## الحادي عشر: التشغيل البيني (Interoperability)

هذا أحد الهدفين الرئيسيين من اقتراح الأطروحة. المشرفة قالت إن النموذج السابق يفتقر لهذا الجانب:

- **REST API محمي بـ JWT**: يسمح لأنظمة المؤسسة الأخرى (HR, ERP) بالتفاعل مع نظام المعرفة
- **Webhooks**: النظام يُرسل إشعارات push لأنظمة خارجية عند حدوث تحديثات
- **Swagger UI**: توثيق تلقائي لكل الـ API endpoints
- **Slack نفسه**: جسر تشغيل بيني بين نظام KM وأدوات التواصل اليومية

---

## الثاني عشر: مقاييس التقييم

| المهمة | المقياس | الهدف |
|--------|---------|-------|
| T1 | MAE (متوسط الخطأ المطلق) | < 0.15 |
| T2 | ROUGE-L (جودة التقارير) | > 0.35 |
| T2 | BERTScore F1 | > 0.80 |
| T3 | دقة اكتشاف التكرارات (Precision) | > 0.85 |
| T3 | Silhouette Score (جودة التجميع) | > 0.50 |
| T4 | دقة اكتشاف التناقضات | > 0.80 |
| T5 | NER F1 | > 0.75 |
| T5 | Lift (قواعد الارتباط) | > 1.5 |

---

## الثالث عشر: كيف يرتبط كل شيء بفصول المذكرة

| الفصل | المحتوى | المصدر |
|-------|---------|--------|
| الفصل 1 | دراسة ببليوغرافية (تعريفات AI، حالة الفن، تقنيات AI×KM) | ملف 1.docx |
| الفصل 2 | عمليات KM (المشاركة من مذكرة إكرام + التحديث المُحسَّن) | المذكرة + 1.docx |
| الفصل 3 | المساهمة (5 مهام + العملية الخامسة + مخططات البنية) | تصميمنا |
| الفصل 4 | التنفيذ (Stack التقني، Docker، الكود، لقطات الشاشة) | الكود الفعلي |
| الفصل 5 | التقييم (نتائج الاختبارات، جداول المقاييس، الرسوم البيانية) | الاختبارات |

---

## الرابع عشر: سيناريو العرض للمشرفة

1. عرض الحاويات: `docker ps` ← كل الخدمات شغالة
2. إدخال وثيقة PDF فرنسية عبر API
3. مشاهدة أحداث Kafka تتدفق
4. سؤال البوت: `@KM Bot quelle est la procédure de sauvegarde?`
5. عرض نقطة التقادم: `/km-predict`
6. عرض تنبيه فوري مع أزرار تفاعلية
7. النقر على "أرشفة" ← التحقق من تغيير الحالة + سجل التدقيق
8. عرض التقرير الأسبوعي في `#km-updates`
9. فتح Neo4j Browser ← تصور العلاقات بين المفاهيم
10. فتح Swagger UI ← إثبات التشغيل البيني
